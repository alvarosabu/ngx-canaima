import { Action } from './../../action';
import { GridListItemBase } from './../grid-list-item-base';
export class GridListItem extends GridListItemBase {
    constructor(
        options: {} = {}
    ) {
        super(options)
    }
}
